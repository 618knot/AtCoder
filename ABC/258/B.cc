#include <bits/stdc++.h>
using namespace std;
 
int main(){
    int N;
    cin >> N;
    int A[10][10];
    for(int i = 0; i < N; i++){
        for(int j = 0; j < N; j++){
            cin >> A[i][j];
        }
    }

    int x[10], y[10];
    int max = 0;
    for(int i = 0; i < N; i++){
        for(int j = 0; j < N; j++){
            if(max < A[i][j]){
                
            }
        }
    }
    return 0;
}